<Window x:Class="Companion.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Companion Settings"
        Width="360"
        Height="390"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <CheckBox x:Name="MischiefEnabledCheck"
                  Grid.Row="0"
                  Content="Enable Mischief"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Auto-off minutes:" VerticalAlignment="Center" Width="150"/>
            <TextBox x:Name="MischiefAutoOffBox" Width="80"/>
        </StackPanel>

        <CheckBox x:Name="CaptureEnabledCheck"
                  Grid.Row="2"
                  Content="Enable Screenshot Capture"
                  Margin="0,12,0,0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Capture min interval (ms):" VerticalAlignment="Center" Width="150"/>
            <TextBox x:Name="CaptureIntervalBox" Width="80"/>
        </StackPanel>

        <TextBlock Grid.Row="4"
                   Margin="0,14,0,0"
                   FontWeight="SemiBold"
                   Text="Global Kill Hotkey"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,8,0,0">
            <CheckBox x:Name="CtrlCheck" Content="Ctrl" Margin="0,0,10,0"/>
            <CheckBox x:Name="AltCheck" Content="Alt" Margin="0,0,10,0"/>
            <CheckBox x:Name="ShiftCheck" Content="Shift"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock Text="Key:" Width="60" VerticalAlignment="Center"/>
            <ComboBox x:Name="KeyCombo" Width="100"/>
        </StackPanel>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Save" Width="86" Margin="0,0,8,0" Click="Save_Click"/>
            <Button Content="Cancel" Width="86" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
